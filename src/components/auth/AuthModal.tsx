"use client"

import {motion} from "motion/react"

type propType = {
  open: boolean,
  onClose: () => void
}

const AuthModal = ({ open, onClose }: propType) => {
  return (
    <>

    </>
  )
}

export default AuthModal